---
import type { ImageMetadata } from 'astro';
import { Image } from 'astro:assets';
import Button from '@/components/ui/Button.astro';
import ProfileAccordion from '@/components/react/ProfileAccordion.tsx';
import type { Doctor } from '@/data/doctors';
import { buildWhatsappUrl } from '@/lib/utils/whatsapp';

/**
 * Card de médico — avatar circular flutuante sobre card claro centralizado.
 * Sem foto: avatar com iniciais na cor da especialidade.
 */
interface Props {
  doctor: Doctor;
}

const { doctor } = Astro.props;

type SpecialtyKey = 'Nefrologia' | 'Endocrinologia' | 'Urologia';

const specialtyConfig = {
  'Nefrologia': {
    specialtyTx: 'text-azure-300',
    avatarBg: 'bg-azure-600',
  },
  'Endocrinologia': {
    specialtyTx: 'text-gold-300',
    avatarBg: 'bg-gold-600',
  },
  'Urologia': {
    specialtyTx: 'text-ink-300',
    avatarBg: 'bg-ink-600',
  },
} as const;

const cfg = specialtyConfig[doctor.specialty as SpecialtyKey] ?? specialtyConfig['Nefrologia'];

const photoModules = import.meta.glob<{ default: ImageMetadata }>(
  '/src/assets/doctors/*.{jpg,jpeg,png,webp}',
  { eager: true },
);
const photoEntry = Object.entries(photoModules).find(([path]) =>
  path.includes(`/${doctor.photoId}.`),
);
const photo = photoEntry?.[1]?.default;

const initials = doctor.name
  .replace(/^(Dr\.|Dra\.)\s*/i, '')
  .split(' ')
  .filter(Boolean)
  .slice(0, 2)
  .map((p) => p[0])
  .join('')
  .toUpperCase();

const waUrl = buildWhatsappUrl(`Olá! Gostaria de falar com a equipe do(a) ${doctor.name}.`);
---

<article class="relative flex h-full flex-col items-center rounded-2xl border border-white/10 bg-ink-900 px-8 pb-9 pt-16 text-center transition-colors duration-300">

  {/* Avatar flutuante */}
  <div class="absolute -top-12 left-1/2 -translate-x-1/2">
    {photo ? (
      <Image
        src={photo}
        alt={`Foto profissional de ${doctor.name}`}
        width={112}
        height={112}
        class="h-24 w-24 rounded-full object-cover ring-4 ring-ink-900"
      />
    ) : (
      <div
        class:list={[
          'flex h-24 w-24 items-center justify-center rounded-full ring-4 ring-ink-900',
          cfg.avatarBg,
        ]}
      >
        <span class="font-display text-2xl font-medium text-white" aria-hidden="true">{initials}</span>
      </div>
    )}
  </div>

  {/* CRM/RQE */}
  <p class="font-sans text-[11px] font-medium tracking-[0.12em] text-ink-300 uppercase">
    CRM {doctor.crm}&thinsp;·&thinsp;RQE {doctor.rqe}
  </p>

  {/* Nome */}
  <h3 class="mt-1.5 font-display text-xl font-medium text-white">{doctor.name}</h3>

  {/* Especialidade */}
  <p class:list={['mt-1 font-sans text-sm font-medium', cfg.specialtyTx]}>{doctor.role}</p>

  {/* Mini-bio */}
  <p class="mt-3 flex-1 font-sans text-sm leading-relaxed text-azure-100">{doctor.miniBio}</p>

  {/* CTA */}
  <div class="mt-6 w-full">
    <Button
      href={waUrl ?? '#contato'}
      target={waUrl ? '_blank' : undefined}
      rel={waUrl ? 'noopener noreferrer' : undefined}
      class="w-full"
    >
      Agendar consulta
    </Button>
  </div>

  {/* Perfil completo */}
  <div class="mt-4 w-full text-left">
    <ProfileAccordion client:visible paragraphs={doctor.fullProfile} doctorName={doctor.name} inverted />
  </div>
</article>
