---
import { cn } from '@/lib/utils/cn';

interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
}

const { as = 'div', class: className } = Astro.props;
const Tag = as;
---

<Tag class={cn('mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8', className)}>
  <slot />
</Tag>
