---
import { cn } from '@/lib/utils/cn';

/**
 * Botão polimórfico: renderiza <a> quando há `href`, senão <button>.
 * `primary` (aqua profundo) é o CTA da marca — funciona tanto nos fundos
 * claros quanto na seção escura de contato.
 */
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'outline';
  size?: 'md' | 'lg';
  type?: 'button' | 'submit' | 'reset';
  class?: string;
  id?: string;
  disabled?: boolean;
  target?: string;
  rel?: string;
  'aria-label'?: string;
}

const {
  href,
  variant = 'primary',
  size = 'md',
  type = 'button',
  class: className,
  disabled = false,
  target,
  rel,
  ...rest
} = Astro.props;

const base =
  'inline-flex items-center justify-center gap-2 rounded-full font-sans font-semibold tracking-wide transition-all duration-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50';

const variants = {
  primary:
    'bg-azure-600 text-white shadow-sm shadow-azure-900/15 hover:bg-azure-500 hover:shadow-md focus-visible:ring-azure-500',
  secondary:
    'bg-ink-900 text-ivory hover:bg-ink-800 focus-visible:ring-ink-700',
  outline:
    'border border-ink-200 bg-white/60 text-ink-900 hover:border-azure-300 hover:bg-azure-50 focus-visible:ring-azure-300',
};

const sizes = {
  md: 'h-11 px-6 text-sm',
  lg: 'h-13 px-8 text-base',
};

const classes = cn(base, variants[variant], sizes[size], className);
---

{
  href ? (
    <a href={href} class={classes} target={target} rel={rel} {...rest}>
      <slot />
    </a>
  ) : (
    <button type={type} class={classes} disabled={disabled} {...rest}>
      <slot />
    </button>
  )
}
